"""
Tab DASHBOARD — KPIs, gráficos de ventas, ranking, métricas por ejecutivo.
Código fuente original: app.py líneas 15992-16497 (with tab_dash)
"""
import streamlit as st


def render_tab_dashboard(supabase, supabase_admin, **deps):
    """
    Renderiza el tab de dashboard con KPIs y gráficos.
    deps: listar_cotizaciones, calcular_estadisticas, ...
    """
    # TODO: mover código de app.py líneas 15992-16497 aquí
    st.info("🚧 Tab dashboard — pendiente de migración desde app.py")
