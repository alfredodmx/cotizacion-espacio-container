"""
Tab EDICIÓN PDF — Visor y edición de PDFs, descripciones por ítem.
Código fuente original: app.py líneas 16498-16670 (with tab6)
"""
import streamlit as st


def render_tab_pdf(supabase, supa_url, supa_key, **deps):
    """
    Renderiza el tab de edición/visor de PDFs.
    deps: cargar_descripciones_por_ep, guardar_descripciones_por_ep, detectar_navegador, ...
    """
    # TODO: mover código de app.py líneas 16498-16670 aquí
    st.info("🚧 Tab PDF — pendiente de migración desde app.py")
