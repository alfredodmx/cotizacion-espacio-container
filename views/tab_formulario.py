"""
Tab FORMULARIO CLIENTE — Config catálogo, preguntas, progreso de respuestas.
Código fuente original: app.py líneas 19193-19329 (with tab_formulario)
"""
import streamlit as st


def render_tab_formulario(supabase, supabase_admin, supa_url, supa_key, **deps):
    """
    Renderiza el tab de formulario cliente (admin).
    deps: build_catalogo_html, build_config_preguntas_html, ...
    """
    # TODO: mover código de app.py líneas 19193-19329 aquí
    st.info("🚧 Tab formulario cliente — pendiente de migración desde app.py")
